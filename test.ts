import tape = require('tape');
import {
    Plane,
    Disk,
    intersectDisk,
    intersectPlane,
} from './src';
import { vec3 } from 'gl-matrix';

tape('cone intersection 1', async (t) => {
    
    t.end();
});